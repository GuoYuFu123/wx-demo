<!--index.wxml-->
<view class="container" id="box">
    <view class="baseinfo">
        <text>session_key: {{session_key}} \n</text> 
        <text>openid: {{openid}} \n</text>    
        <text>access_token: {{access_token}}</text> 
        <image wx:if="{{imgSrc}}" src="{{imgSrc}}"  style="width: 100px;height: 100px;"/>
    </view>
    <view>
        <i-button  open-type='share'>分享</i-button>
    </view>

</view>
